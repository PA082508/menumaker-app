# Что НЕ загружать в сборку Vercel.
#
# ПОВОД (02.08): деплой отказал — «File size limit exceeded (100 MB)», выгрузка
# доросла до 831 МБ. Виноват `demo-out/` — 753 МБ сырых видео с записи демо, из них
# один файл 261 МБ. В `.gitignore` он есть, но правило .gitignore распространяется
# на ГИТ, а не на выгрузку CLI; своего списка у выгрузки не было вовсе, поэтому она
# несла на сервер всё, что лежит в папке проекта.
#
# Здесь только локальные артефакты. Ничего, что нужно сборке (src, public, index.html,
# vite.config.ts, package.json), в списке нет и быть не должно.

.demo-profile/
demo-out/
smoke-out/
node_modules/
dist/
import/
.git/
