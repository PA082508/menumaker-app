# Принудительное обновление, личность устройства, дверь B (01.08.2026)

**Три работы из отбора владельца. ✅ ВЫКАЧЕНО по слову 01.08**, коммит `7442dee`,
живая сборка `7442dee` (read-back — §5). Сборка зелёная: `tsc` чист, **318 тестов**,
`vite build` собирается.

---

# 0. 🔴 ОТВЕТ НА БЛОКИРУЮЩИЙ ВОПРОС: ДО-10.07 КЛИЕНТ НОВЫЙ КЛЮЧ **НЕ ПЕРЕЖИВЁТ**

**Вопрос владельца:** переживёт ли до-`f25d55d` клиент миграцию `20260731a`? Куда целится его
запись — в конкретный `id` или в `onConflict` по снятому ограничению?

**Ответ: в ОБА, и это худший из вариантов.** У старого `toggle` две ветки
(`MealCountPage.tsx` до `f25d55d`, строки 320–334):

| Ветка | Куда целится | Переживёт миграцию? |
|---|---|---|
| строка уже есть (`existing?.id`) | `.update(...).eq("id", existing.id)` — **конкретный id** | ✅ да, идентичность строки её не касается |
| строки нет | `.upsert(..., { onConflict: "classroom_id,child_name,monday_date" })` — **ровно снимаемый индекс** | ❌ **нет** |

## 0.1 Проба, а не рассуждение

Прогнал на **временной** таблице в живой базе (боевых таблиц не касался): два частичных
индекса ровно как в `20260731a`, затем запись старого клиента.

```
ОТКАЗ · SQLSTATE 42P10 · there is no unique or exclusion constraint
                         matching the ON CONFLICT specification
КОНТРОЛЬ (схема сегодня): ПРОШЛО
```

**Причина точная.** Миграция заменяет обычный индекс `meal_week_records_unique`
`(classroom_id, child_name, monday_date)` на **два ЧАСТИЧНЫХ**. Postgres выводит частичный
индекс в `ON CONFLICT` **только если в запросе указан тот же предикат** (`... WHERE roster_id
is null`). У старого клиента предиката нет и быть не может — он написан до появления частичных
индексов в этой таблице. Голый трёхколоночный target перестаёт совпадать **с чем бы то ни было**.

Проверено, что индекс сегодня ещё на месте: `20260731a` **не применена**,
`meal_week_records_unique` жив, частичных индексов в базе нет.

## 0.2 Насколько часто срабатывает гибнущая ветка — измерено

Строки недели создаются **не пачкой при открытии недели, а постепенно, по первому тапу
на ребёнка**:

| Центр | Неделя | Строк | Различных минут создания | Первая → последняя |
|---|---|---|---|---|
| **Ridge** | 27.07 | 132 | **40** | пн 08:22 → чт 15:36 |
| Ridge | 20.07 | 136 | 35 | пн 08:11 → пт 16:12 |
| Highland | 27.07 | 82 | 32 | пн 09:47 → пт 16:22 |

**Неделя 03.08 сегодня почти пуста — 1 строка** (и та от браузера проверок). Значит в
понедельник у Ridge гибнущая ветка сработает на **первом тапе по КАЖДОМУ ребёнку** — это
порядка **140 отказов подряд**, весь центр.

## 0.3 🔴 И отказ будет НЕВИДИМЫМ

Старый клиент глотает ошибку:

```ts
} catch {
  setRecords(prev => ({ ...prev, [child.child_name]: { ...prev[...], [col]: current } }));
}
```

**Ни сообщения, ни звука.** Галочка просто отскочит обратно. Повар увидит не «система не
работает», а «планшет глючит», и будет тыкать снова. Это хуже видимого отказа: видимый отказ
зовут чинить, невидимый — обходят.

## 0.4 Вывод — порядок меняется, как владелец и предполагал

```
❌ было:  (2) схлопывание сирот → (1) миграция ключа
✅ стало: планшет Ridge РУКАМИ → (2) схлопывание → (1) миграция ключа
```

**Миграция не применена и не будет применена до подтверждения, что Ridge на новой сборке.**
Подтверждение машинное и однозначное: у отметок Ridge появится `app_version`.

## 0.5 Других писателей со старым ключом не осталось — проверено

| Где искали | Результат |
|---|---|
| Функции БД, трогающие `meal_week_records` (8 шт.) | голый старый target **только** у `sync_meal_marks` — и её `20260731a` заменяет в том же файле |
| Сегодняшний клиент | прямых `insert`/`upsert` в `meal_week_records` **нет вовсе**, всё через RPC |
| `MealCountDirectorPage.tsx:248` | голый старый target **есть**, но файл **мёртв** (обе двери рендерят `MealCountPage`). Писать не может, но при оживлении сломается — мина, а не пожар |

---

# 1. ПРИНУДИТЕЛЬНОЕ ОБНОВЛЕНИЕ PWA

## 1.1 Дыра оказалась не там, где я её называл

Разбирая по исходникам, а не по памяти: `registerType:'autoUpdate'`, `skipWaiting` и
`clientsClaim` в `vite.config.ts` **уже стояли**, и клиент vite-plugin-pwa в режиме auto
**сам перезагружает страницу** на событии `activated` с `isUpdate`
(`node_modules/vite-plugin-pwa/dist/client/build/register.js`).

**Не хватало не умения обновляться, а момента, когда клиент вообще смотрит.** Браузер ищет
новую версию service worker'а только **при регистрации** — то есть при загрузке страницы — либо
по явному `registration.update()`. Планшет в группе не закрывают неделями: регистрация не
повторяется, `update()` не зовёт никто, **новая сборка не ищется никогда**. Ridge отставал не
потому, что обновление сломано, а потому, что его никто не запрашивал.

## 1.2 Сделано — две независимые дороги, нарочно

| | Дорога | На что опирается |
|---|---|---|
| 1 | `registration.update()` по таймеру (30 мин), при возврате на вкладку и при появлении сети | на **исправный** service worker |
| 2 | Сверка версий: `/version.json` с сервера против исполняемой `__BUILD_ID__` → красная полоса «Приложение устарело» с кнопкой | **ни на что, кроме сети** |

Одной дороги (1) мало: она чинит нормальный случай молча, но бессильна там, где SW залип или
его нет вовсе, — а Ridge был именно этим случаем. Дорога (2) ловит ровно его.

`/version.json` генерируется плагином в `vite.config.ts` и **гарантированно идёт в сеть**:
workbox прекэширует только `js/css/html/ico/png/svg/woff`, json туда не попадает. Проверено на
собранном `dist/sw.js` — упоминаний `version.json` в precache-манифесте **ноль**.

Кнопка «Обновить» не просто перезагружает: сначала `registration.update()`, затем чистка Cache
Storage, затем reload. **Без чистки кэшей перезагрузка залипшего клиента вернула бы ту же
старую страницу из precache** — выглядело бы как обновление, не обновив ничего. Очередь отметок
живёт в IndexedDB и чисткой кэшей **не задевается**; офлайн чистка не делается вовсе, иначе
человек остался бы без приложения.

## 1.3 🔴 Чего это НЕ чинит — сказано прямо

**Ни одна из двух дорог не достаёт до уже устаревшего клиента.** Код, исполняющийся сегодня на
планшете Ridge, написан до 10.07 и о существовании этой полосы не знает. **Ridge чинится
руками.** Написанное сегодня существует, чтобы **следующий** отставший нашёлся сам.

Полоса **не блокирует** экран: пока человек не нажал, он должен продолжать отмечать. Старая
сборка пишет хуже (без строки журнала), но пишет; отнять инструмент посреди обеда дороже суток
на старой версии. Блокировать станет уместно **после** закрытия прямой записи на уровне прав
(работа 1 из §7.2 плана 31.07d) — тогда старый клиент перестанет сохранять вообще, и молчать
будет нельзя.

**Файлы:** `src/lib/appUpdate.ts` (новый) · `src/components/AppUpdateBanner.tsx` (новый) ·
`vite.config.ts` · `src/main.tsx` · `src/App.tsx`.

---

# 2. ЛИЧНОСТЬ УСТРОЙСТВА В IndexedDB

`device_id` переехал в то же IndexedDB, где живёт очередь; `localStorage` остался **зеркалом**.
Порядок разрешения: IndexedDB → зеркало → выдать новую. Устройства, работавшие до правки,
**сохраняют прежний id** — он переносится из зеркала, история отметок не рвётся.

## 2.1 ⚠️ Поправка к обоснованию из плана 31.07d §19.3

Там сказано: «iOS чистит `localStorage` установленного PWA» — как будто IndexedDB чистится
иначе. **Это неточно.** WebKit удаляет всё скриптовое хранилище **одним пакетом**
(localStorage, IndexedDB, Cache Storage, регистрации SW); исключение даёт не выбор хранилища,
а **установка приложения на домашний экран**.

**Переносом id в IndexedDB мы не покупаем защиту от вытеснения.** Покупаем две вещи, обе
настоящие и обе проверены пробами:

1. id **переживает недоступный `localStorage`** (WebView, приватный режим) вместо схлопывания
   в `'unknown'` — раньше все такие устройства сливались в **одну** «личность»;
2. id вытесняется **вместе с очередью**, а не отдельно от неё — исчезает состояние «отметки на
   месте, а устройство уже другое», в котором данные врут **связно**.

Проверка на живом iPad остаётся человеческой частью пробы. Машина этого не подтверждает, и
инструкция это говорит.

## 2.2 Пробы

**+4 автопробы, все с негативной пробой** — на вчерашнем коде краснеют все четыре:
переживает перезапуск · заблокированный `localStorage` больше не даёт `'unknown'` · прежний id
переносится · при расхождении главным остаётся IndexedDB, зеркало чинится.

**Файлы:** `src/lib/mealMarkQueue.ts` · `src/lib/mealMarkQueue.offline.test.ts` (7 проб).

---

# 3. QUICK ADD — ВАРИАНТ (б), ДВЕРЬ B ПИШЕТ ТОЛЬКО `P`

## 3.1 Течь была шире, чем в оценке

Дверь B — модалка **«➕ Add Child»** (`CenterRosterPage.tsx`): прямая вставка в ростер, носитель
`income_eligibility` не пишется, а заявку считает **носитель**. Отсюда 21 ребёнок, который
числится Free, а июль считает Paid.

🔴 **Найдено при правке и в оценке не значилось: умолчанием поля стояло `frp: 'F'`.**
То есть Free выдавалось **без единого действия директора** — достаточно было нажать «Add Child».

## 3.2 Сделано

Выбор F/R убран **совсем**, а не оставлен с предупреждением: предупреждение не мешает нажать,
а цена нажатия — расхождение витрины с деньгами. Категория здесь одна — `P`, и это не суждение
о ребёнке, а честное «определения ещё не было». На месте действия сказано словами, где
категория назначается по-настоящему.

**Цена варианта (б) названа вслух:** директору с бумагой на руках придётся зайти вторым заходом
в карточку ребёнка. Вариант (а) — следующим заходом со своим read-back.

**Гард** `quickAddPaidOnly.guard.test.ts`, 4 пробы, все красные на вчерашнем коде. Правило
дешевле держать в сборке, чем в памяти: вернуть `<option value="F">` — правка на одну строку,
выглядящая как улучшение, и она ничего не сломает **видимо** — разойдётся только заявка, через
месяц.

---

# 3.3 🔴 READ-BACK ВСКРЫЛ ВТОРУЮ ДВЕРЬ ТОЙ ЖЕ ТЕЧИ — НЕ ТРОНУТА, ЖДЁТ СЛОВА

Сверяя выкаченный бандл, нашёл **второе** место, где `Meal Status (FRP)` предлагает Free/Reduced:
**«Ручной ввод»** (`ManualEntryModal`, `AddChildRouter.tsx:325`). Оно подаёт `pending`-заявку,
которую директор потом утверждает в Inbox, — и вот что с ней дальше:

| Кто зовёт `recordDetermination` (пишет носитель) | |
|---|---|
| `ChildSettingsPage.tsx:436` — карточка ребёнка | ✅ дверь A |
| `enrollmentApprove.ts:690` — апрув **IEA** | ✅ дверь A |
| `approveCacfpInsert` — апрув ручного ввода / скана | ❌ **НЕ зовёт** |

Значит `F`, выбранная в ручном вводе, доходит до `roster.frp` и **не доходит до носителя** —
ровно та же течь, что закрыта сегодня в Quick Add, только вторым ртом.

**Почему это менее срочно, но не пустяк:** там умолчание — `'P'`, а не `'F'`, поэтому «само»
Free не выдаётся; нужно осознанно выбрать. Но выбрать можно, и результат будет тот же:
на экране Free, в деньгах Paid.

⛔ **Не трогал.** Это шаг 2 варианта (а) из §18 плана 31.07c (`approveCacfpInsert` →
`recordDetermination`, 0.5 дня) и он идёт со своим read-back, а не в хвосте субботней выкатки.

---

# 4. CHANGELOG — СТРОКА ВСТАВЛЕНА ПРИ ВЫКАТКЕ

Ушла в `docs/CHANGELOG.md` вместе с коммитом `7442dee`. Черновой вариант, из которого она
собрана, оставлен здесь как след:

> `- 2026-08-01 · 🚀 · ` Приложение теперь само замечает, что устарело. Планшет, открытый в
> группе неделями, никогда не спрашивал сервер о новой версии — браузер ищет её только при
> загрузке страницы, а страницу там не перезагружают; именно так один планшет Ridge месяц
> писал отметки без времени подачи. Теперь версия проверяется каждые полчаса, при возврате на
> экран и при появлении сети, а если сервер отдаёт сборку новее исполняемой — сверху появляется
> полоса «Приложение устарело» с кнопкой, которая честно сносит кэш и забирает новую. Отметки
> при этом не теряются, они лежат в другом хранилище. Сказано и то, чего это не чинит: до уже
> отставшего планшета ничего из выкаченного не дотянется — он обновляется руками. Личность
> устройства переехала туда же, где лежит очередь отметок: раньше при заблокированном хранилище
> все такие устройства сливались в одно «неизвестное», а весь разбор «кто отмечает задним
> числом» держится на нём одном. И быстрое добавление ребёнка перестало раздавать категорию
> Free: эта дверь не пишет носитель определения, поэтому Free с неё виден на экране, но в
> деньги не попадает — а стояла она там ещё и умолчанием, то есть выдавалась сама.


---

# 5. READ-BACK НА ЖИВОМ — ПОСЛЕ ВЫКАТКИ (01.08)

| Что проверялось | Как | Результат |
|---|---|---|
| Выкачена именно эта сборка | `GET /version.json` | `{"build":"7442dee","sha":"7442dee53a0b…"}` ✅ |
| Версия в подвале боковой панели | `7442dee` в живом бандле | найдено, 3 вхождения ✅ |
| Полоса «Приложение устарело» | строка в живом бандле | найдена ✅ |
| Quick Add без выбора F/R | «Quick Add always files a child as» + блок `Paid (P)` вместо `<select>` | найдено ✅ |
| Сверка версий не отравлена кэшем | `/version.json` в precache-манифесте живого `sw.js` | **0 вхождений** ✅ |

⚠️ **Чего эта сверка НЕ доказывает.** Она доказывает, что **код доехал**, а не что человек
увидит правильную картинку. Полоса обновления в живой природе **не показывалась ни разу** —
чтобы её увидеть, нужна вторая выкатка поверх открытой вкладки. Проверка глазами остаётся за
человеком, и по правилу закрытия находок эта находка закрывается **только его сверкой**, не
моей.

---

# 6. 🔴 СХЛОПЫВАНИЕ СИРОТ ОСТАНОВЛЕНО: ПРАВИЛО ПОБЕДИТЕЛЯ БЫЛО ОБРАТНЫМ (01.08)

**Проверка, которую владелец поставил условием, вернулась отрицательной по ВСЕМ группам.**

`20260731b` оставляет победителем самую раннюю строку (`order by created_at, id`) и написание
имени не смотрит вовсе. Замер по 24 группам:

```
🔴 24 из 24 — у победителя написание НЕ РОСТЕРНОЕ. Совпадений НОЛЬ.
```

Это не совпадение, а причинно-следственная связь: июньский кухонный импорт написал
«Имя Фамилия» и создал строку **раньше**; экран ищет по ростерному «Фамилия Имя», не находит,
повар отмечает неделю заново — и ростерная строка рождается **позже**. Правило «кто раньше»
поэтому систематически выбирает импортную строку. Примеры: `Andras Inara` (ростер) против
`Inara Andras` (победитель) · `Rakhmanov Erulan` против `Rackmanov Erulan` ·
`Graves Teighan` против `«Graves  Teighan »` (двойной пробел).

## 6.1 Область больше, чем «старый планшет Ridge»

Экран индексирует строки **по имени** — и старый, и **сегодняшний**
(`MealCountPage.tsx:351,365`, `map[r.child_name]`).

| Клиент | Что было бы в понедельник |
|---|---|
| **старый** (Ridge) | неделя выглядит пустой → повар отмечает заново → `upsert` создаёт **новую сироту**. Чистка воспроизвела бы то, что чистит |
| **новый** | новой сироты не будет (после `20260731a` ключ на `roster_id`), но экран покажет **пустые клетки при отмеченной строке** — ложь экрана. Тоже недопустимо: повар верит экрану |

## 6.2 Подписи от смены победителя не страдают — проверено

| Написание | Строк | С подписью директора |
|---|---|---|
| импортное (сегодняшний победитель) | 25 | **24** |
| ростерное (сегодняшний проигравший) | 23 | **22** |

Подписаны **обе** стороны. Кто бы ни выжил, проигравший уезжает в архив дословно вместе с
подписью — механика сохранена. Подписанное не переписывается, добавляется запись о слиянии.

## 6.3 Что подготовлено: `20260801c` вместо `20260731b`

Правило победителя: `order by (child_name = roster.child_name) desc, created_at, id` — ростерное
написание побеждает всегда. Плюс **новая самопроверка перед коммитом**: если хоть у одной
выжившей строки написание не ростерное — исключение и откат. Возражение владельца превращено
в инвариант файла, а не в обещание.

## 6.4 🔴 ОДНА ГРУППА ПРАВИЛУ НЕ ПОДЧИНЯЕТСЯ — НУЖНО СЛОВО

```
Ridge · Orange 2 · неделя 06.07
  ростер:   «Rodriguez- Texidor Izabella»   ← лишний пробел после дефиса
  строка 1: «Izabella Rodriguez-Texidor»
  строка 2: «Rodriguez-Texidor Izabella»
```

Ростерному написанию не равна **ни одна** из двух. Файл эту группу **намеренно пропускает** и
говорит об этом вслух — молчаливый пропуск читался бы как «таких не было». Следствие: гард
`20260731a` («расщеплений не осталось») ключ не пропустит, пока человек не решит.

Два пути, оба требуют слова и ни один не делается миграцией втихую:
**(а)** поправить ростер (убрать лишний пробел) — это правка имени ребёнка;
**(б)** признать победителем `Rodriguez-Texidor Izabella` и привести ростер к ней.

**Рекомендую (а):** лишний пробел в `Rodriguez- Texidor` — явная опечатка ввода, а не вариант
написания фамилии, и чинить надо источник, а не следствие.
