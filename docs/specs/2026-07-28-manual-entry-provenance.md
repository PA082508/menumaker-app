# Ручной ввод и провенанс — заход 1 (ядро)

**Статус: СПЕКА, стройка НЕ начата. Ждёт слова владельца.**
Заказ Николая 28.07.2026. Замер (заход 0) — в этом же файле, раздел «Измерено».

Электронная половина модели провенанса (провод `form_version`, поле происхождения) и
бумажная (ручной ввод) сведены в один заход по решению владельца: **запись у них одна**,
и строить порознь значит спроектировать её дважды.

---

## Ратифицированные правила

### 1. Данные сверх формы не исключаются и не удаляются

Значение, источник которого больше не собирается, **не неверно — оно заморожено**. Удаление =
потеря реальной информации и прямое нарушение канона «чистка данных = обновление, не удаление».

Решение — **провенанс**: показывать **источник** и **документную дату**, чтобы состояние
«пришло из старой формы, новой не обновляется» было **видимым**, а не молчаливым.

Существующие данные занесены по старой форме; в новых редакциях полей **меньше**. Это
ожидаемое и допустимое состояние, а не дефект данных.

### 2. Правило порта: отсутствие поля ≠ пустое значение

**Порт пишет только те поля, которые форма реально несёт.** Поля, которых в форме нет,
**не трогаются** и никогда не затираются `NULL`'ом.

Проверка живых путей (28.07) — см. «Измерено → правило порта».

### 3. Три состояния поля и красный счётчик

Канон вынесен в [`platform-standards.md`](../platform-standards.md) — «Красный счётчик обязан
быть обнуляемым». Признак живёт на `FieldDef` в едином реестре полей, значение выводится
из обратного замера (ниже).

### 4. Три уровня замка

🔒 заперто (только документ) · ⚠ метится (со слов можно, маркер обязателен) · свободно.
Медицинское **не запирается**: аллергия, узнанная по телефону в 9 утра, должна записываться
в 9 утра. Список полей — раздел «Замок».

### 5. Что заход 1 несёт

Журнал событий обновления · провенанс · провод `form_version` и поля происхождения ·
источник (форма библиотеки / свободный документ / со слов) · **документная дата и дата ввода
порознь** · кто вносил · что изменилось · применение значения **строго по документной дате** ·
видимый маркер «со слов, без документа». **Без сканов** — этим уже можно вводить бумажный
бэклог. Замок (заход 1а) прицеплен сюда же.

---

## Измерено (read-only, 28.07.2026)

### Реестр полей

`src/lib/childFieldRegistry.ts` → **31 `FieldDef`** (не 34): roster 17 · child_medical 14 ·
`v_child_age_profile` 2 read-only. Плюс производные правила Family (2 единицы) и SafePass (1).

### Истории изменений нет

**0 триггеров** на `roster`, `child_medical`, `child_guardian`, `income_eligibility`.
Таблицы audit/history нет. `roster.updated_at` поддерживается приложением; что именно
менялось — не знает никто (149 строк из 623 обновлялись после создания).

Прецеденты-журналы, все написаны от руки: `roster.admission_log` (3 непустых из 623) ·
`income_eligibility.determination_log` (45 из 576) · `roster.sched_source/_updated_by/_updated_at`
(275 строк).

**Документная дата уже реализована на одной группе полей:** `formAsOf()` =
`signature_date ?? created_at`, плюс `scheduleIsStale()` с правилом Николая от 16.07 — «на
расхождении побеждает более поздняя дата; равные — форме, она подписанный документ». Заход 1
её **обобщает**, а не изобретает.

### Поле происхождения уже существует и пусто

`roster.source` заполнен у **9 из 318** активных детей (`masterlist_fiscal` 7, `food_import` 2).
У 309 — `NULL`.

### Правило порта — состояние живых путей

- **`buildCacfpPatch` — чист**, каждое поле под `if (!blank(...))`. **Одно исключение:**
  `child_name` пишется безусловно и при форме без имени затёр бы его пустой строкой. Сегодня
  не долетает только потому, что `approveCacfpUpdate` вырезает `child_name` из патча по
  **другой** причине (защита claim-bridge). Ловушка латентная: снимут защиту — вернётся.
- **`buildIeaFrp` — чист** (честно возвращает `null`). **Но `approveIea:502` пишет
  `{frp, frp_expires}` безусловно** всем сопоставленным детям. `frpExpiryDefault` никогда не
  возвращает пустое, поэтому `NULL` долетает только если директор **руками очистит** поле даты
  в панели — тогда существующий `frp_expires` затирается. Узко, но активно, и на 🔒-поле клейма.

### Обратный замер: поля-сироты

Активных детей — **318**. «Собирает» = хоть одна действующая форма несёт поле;
«потребляет» = существует код, кладущий его в карточку.

| Поле | Заполнено | Собирает форма | Потребляет | Состояние |
|---|---|---|---|---|
| `first_name` / `last_name` | 309 / 309 | да | да | 1 |
| `birthday` 🔒 | 300 | да | да | 1 |
| `classroom_id` 🔒 | 317 | да (ручной ввод) | да | 1 |
| `date_in` 🔒 | 157 | да | да | 1 |
| `frp` 🔒 | 315 | да | да | 1 |
| `child_address` | 47 | да (`dcy_01234`, `enroll`) | частично | 4 |
| `enrollment_reviewed_at` | 13 | да (`dcy_01234` pg_rev/adm_rev) | **нет** | 4 |
| `emergency_transport_auth` 🔒 | 318 — **все `true`** | да (`dcy_01234` trans_*) | **нет** | 4 ⚠ |
| `has_health_condition` | 318 — 316 `false`, 2 `true` | да (`dcy_01234` health_*) | **нет** | 4 ⚠ |
| `development_notes` | 0 | да (`dcy_01234`) | **нет** | 4 |
| `accommodations` | 0 | да (`dcy_01234`) | **нет** | 4 |
| `specialized_services` | 0 | да (`dcy_01234`) | **нет** | 4 |
| `frp_expires` 🔒 | 132 | да (`iea` sponsor) | да | 1 |
| `date_out` | 0 | **нет** | — | 3 |
| `milk_kind` | 292 | **нет** | — | 3 ⚠ |
| `doctor_name` | 60 из 70 | **нет** | — | 3 |
| `doctor_phone` | 49 из 70 | **нет** | — | 3 |
| `allergies` | 18 из 70 | **нет** | — | 3 |
| `medications` | 10 из 70 | **нет** | — | 3 |
| `parent_signed_at` | **0** | **нет** | — | 3 |
| 8 полей DCY 01236 | **0** | **формы не существует** (`"PENDING"`) | — | 3 |
| `physician_signature_date` | 0 | **нет** | — | 3 |
| Family: полный опекун | 5 | да (`dcy_01234` p1/p2, `enroll`) | **нет** | 4 |
| Family: экстренный контакт | 130 | да (`dcy_01234` ec1/ec2) | **нет** | 4 |
| SafePass: `can_pickup` 🔒 | 241 | да (`child_release_authorization` `pickup[]`) | **нет** | 4 |

Состояния: **1** = собирается и потребляется · **3** = никем не собирается ·
**4** = собирается, но порт не построен.

**Две аномалии, помеченные ⚠ — «заполнено» здесь хуже, чем «пусто»:**

- `emergency_transport_auth` = `true` у **всех 318**. Это не собранный ответ, а сплошной
  дефолт — на 🔒-поле, которое означает **авторизацию** родителя на экстренную перевозку.
  Красного не даёт, потому что «заполнено», но значение — вымысел.
- `has_health_condition` = `false` у 316 из 318 — тот же дефолт с двумя ручными правками.
- `milk_kind`: значения `1pct` (257) и `red` (35) — **словарь из старого импорта кухни**,
  которого нет в `MILK_OPTIONS` карточки (`whole|1%|skim|soy|none`). Поле считается
  заполненным, но выбранного значения в списке нет ни одного.

### Смесь порядков имени НЕ заморожена — она растёт

Оба INSERT-пути ростера (`insertRosterChild`, панель «Добавить ребёнка» в `CenterRosterPage`)
пишут `child_name` в порядке **«Имя Фамилия»**, а 519 легаси-строк лежат **«Фамилия Имя»**.
Значит расхождение не статичный исторический артефакт: **каждый новый ребёнок добавляет
строку в меньшинство**, и так до шага (3) — перевода `cellKey` с `child_name` на `roster.id`.

Не срочно (запись ключа при INSERT безопасна — строк питания у нового ребёнка ещё нет), но
названо: чем позже шаг (3), тем больше строк придётся учитывать при любой будущей
нормализации.

### Ключевой разрыв Family / SafePass

`child_guardian.child_id` ссылается на **`menumaker.child(id)`**, а не на `roster.id`.
У **72 из 318** активных детей `roster.child_id = NULL` → опекунов не может быть в принципе,
и три единицы бейджа на каждого не снимаются **никаким** действием директора. Лечится
key-backfill'ом из карты [права забирать v4.1](../..) (шаг A0), не кодом карточки.

### Размер красного бейджа сегодня

| Категория | Единиц | Доля |
|---|---|---|
| **1 — снимается действием директора** | **201** | **12 %** |
| 4 — собирается, но порт не построен (наша стройка) | 1154 | 68 % |
| 3 — никем не собирается (бэклог) | 344 | 20 % |
| **Всего по 318 активным детям** | **1699** | ≈ 5.3 на ребёнка |

**88 % сегодняшнего красного — не недостача директоров, а наш пробел.**

**Лучшее доказательство, что дефолт был вымыслом:** из **двух** детей, по которым у системы
вообще есть настоящий ответ на разрешение экстренной перевозки, **одного она переворачивает**.
Подписанная 15.07 форма Izabella говорит `trans_no = Yes` — авторизации нет; карточка
утверждает `true`. Не «пробел», а обратное значение на поле авторизации.

> **Обоснование пересчёта бейджа, одной строкой (Николай, 28.07):**
> **1154 единицы из 1699 — состояние 4.** Один день работы прекращает ложь
> в восьми случаях из девяти.

Отдельно ратифицировано: **три единицы Family/SafePass у 72 детей с `roster.child_id = NULL`
считать НЕЛЬЗЯ** — действия у директора нет в принципе, пока не пройдёт key-backfill (шаг A0
карты права забирать). Это не состояние 4 и не 3, а **структурная невозможность**: опекуна
нельзя привязать к строке, у которой нет ключа в `menumaker.child`.

### Doc Hub

`claim_packet_manifest` по Ridge за июль 2026: **28 типов из 28 `present = false`**, 20 из них
обязательные. Подать нельзя **ни один**: в приложении нет пути записи в `menumaker.documents`
(таблица пуста, 0 упоминаний в коде), а вкладка Documents ребёнка грузит файлы в бакет
`org-files` вообще мимо базы. Детских типов в `document_types` нет — все 28 центрового и
спонсорского уровня.

Честное число сегодня — **0 в счёте** при видимом списке отслеживаемого, с причиной.

---

## Замок — утверждённый список

**🔒 Заперто (только документ):** `birthday` · `frp` · `frp_expires` · `date_in` · `date_out` ·
`classroom_id` · `physician_signature_date` · `parent_signed_at` · `emergency_transport_auth` ·
`can_pickup`.

**⚠ Метится (со слов можно, маркер обязателен):** `allergies` · `medications` ·
`foods_to_avoid` · `activities_to_avoid` · `has_health_condition` · `doctor_name` ·
`doctor_phone` · `first_name` · `last_name`.

**Свободно:** `child_address` · `milk_kind` · `development_notes` · `accommodations` ·
`specialized_services` · заметки.

Замок стоит **в save-пути**, не в инпуте — см. канон «правило, живущее только в UI, — не
правило».

---

## Порядок (утверждён 28.07)

точный маппинг ✓ → **заход 1 + 1а** → право забирать v4.1 (C1–C4) → заход 2 (скан + хэш) →
заход 4 (акты типа «б»). Волна 2 и смоук рейса идут параллельно.

## Размер

| Шаг | Оценка |
|---|---|
| Заход 1 (журнал + провенанс + провод) | 2–3 дня |
| Заход 1а (замок) | +½ дня |
| Порт `dcy_01234` в карточку (81 ключ → ~12 полей + 2 контакта + 2 опекуна) | +1–1.5 дня |
| Пересчёт бейджа: карточка | ½ дня |
| Пересчёт бейджа: Doc Hub (нужна колонка «можно подать сегодня» в `document_types`) | ½ дня |
| Заход 2 (скан + хэш, носитель `documents` уже есть) | ½–1 день |
| Заход 4 (акты «б», в основном данные) | ~1 день |

Итого ≈ **6–8 дней**.
