# Teacher Portal — ЗАКАЗ (спека пишется ПОСЛЕ репетиции пн 20.07)

Это не спека. Это зафиксированный заказ Николая от 18.07.2026 + замеры базы,
сделанные в тот же день, чтобы спека начиналась с фактов, а не с допущений.
Тестовый класс — **Red (Ridge)**, 9 детей.

**Вход спеки:** выводы полевого теста (а) на репетиции пн 20.07 (Carolyn + Maureen).
До репетиции спека не пишется.

---

## Состав MVP

| # | Модуль | Статус | Опора |
|---|---|---|---|
| а | Чек-ин/аут **детей** | расширяем | существующий контур `safepass_confirm_handoff` |
| б | Чек-ин/аут **учителя** | новое (обобщение) | механика Cover class → staff-presence по PIN; это и есть обещанный Timesheet из профиль-модала |
| в | Attendance-сетка с аватарками | расширяем | камера-фича + `v_meal_grid` |
| г | Meal Count | есть | вариант существующей страницы |
| д | **Сценарии уроков** (lesson plans) | НОВОЕ | модели нет |
| е | Общение с родителями | расширяем | Internal Messages + Parent PWA |
| ж | Рассылки фото | расширяем | камера-фича, скоуп строго свой класс |
| з | Полный список кандидат-функций | — | таблица «есть / расширяем / новое» на ратификацию |

## д. Сценарии уроков — отдельная секция спеки

- Модель данных: план на **класс × неделю**.
- Редактор учителя, шаблоны.
- **Доступ: только Lead Teacher** (по `staff.position`), создание/редактирование.

**Вопрос Николаю:** ассистенты и вторые учителя класса — read-only или без доступа?

⚠️ **Замер, который влияет на гейт (18.07):** `position` заполнена **не у всех**.
Живых сотрудников 75; `position IS NULL` у **41** из них — Highland 23 из 24,
Pearl 14 из 16, Ridge 4 из 35. `Lead Teacher` проставлен у 9 человек.
Гейт по `position` сегодня отрезал бы больше половины штата. Спека обязана
предложить, что делать: добить `position` данными до запуска, или ввести явный
флаг «может вести планы», или и то и другое. Молча гейтить нельзя.

## е. Общение с родителями

- Поверх существующих Internal Messages: расширение адресатов на guardian'ов
  **своего** класса.
- Поверхность родителя = Parent PWA.

**Вопрос Николаю:** каналы доставки — PWA, email или оба?

## ж. Рассылки фото — гейт СНЯТ (решение Николая 18.07)

Photo-release согласия есть у **всех** детей в старых бумажных пакетах. В системе
их нет, и **искать не надо** — бумажное покрытие полное.

1. Рассылки фото **НЕ гейтятся на запуске**.
2. **Переходный режим:** дефолт «разрешено» + ручной opt-out директором
   (новое поле в карточке ребёнка).
3. В онлайн-контур заполнения (пакет / renewal FY2027) заложить photo-release
   как **отслеживаемое per-child поле** — отдельная форма или галочка в консенте,
   выбор за спекой. Когда онлайн-статусы наполнятся, гейт переключается
   с дефолта на поле.

Риск-рамка, из которой это выросло: групповые фото без release — риск,
индивидуальные фото своему же родителю — ок.

## з. Таблица кандидат-функций

Собрать в спеке одной таблицей «что есть / что расширяем / что новое»
на ратификацию Николая.

---

## Замеры базы 18.07 — входные факты для спеки

| Факт | Значение | Следствие |
|---|---|---|
| `staff.pin_hash` заполнен | **0 из 75** во всех центрах | (б) стартует с нуля; PIN'ы — ручной шаг Николая |
| `staff.classroom_id` заполнен | Highland 0/24 · Pearl 0/16 · Ridge 2/35 | привязка учителя к классу почти отсутствует — несущая ось (а),(б),(д),(е),(ж) |
| Carolyn + Maureen привязаны к Red | **нет** (живые записи без класса) | блокер репетиции → prepare `20260718d` |
| `staff.position` пуста | 41 из 75 | гейт (д) по позиции сегодня нерабочий |
| SafePass устройств/сессий | 0 | (а) — действительно первый полевой тест |

**Главный риск, который спека должна взять первой строкой:** Teacher Portal весь
стоит на связке «учитель ↔ класс». Сегодня эта связка заполнена у 2 сотрудников
из 75. Без плана её наполнения любой модуль раздела — демо на одном классе.

---

# Ратификация Николая 18.07 — ответы по данным

## Двойное представление «учитель ↔ класс» — не противоречие, а два поля

Экран класса Николая показывает `Teachers: Maureen, Carolyn`, а мой замер говорил
«к Red не привязан никто». Оба верны — это **два независимых представления одной
связи**, и они разошлись:

| | поле | тип | кто читает |
|---|---|---|---|
| **для глаз** | `staff.class_primary` | TEXT, имя комнаты | ВСЕ экраны с «Teachers» |
| **для механики** | `staff.classroom_id` | FK uuid | **никто** |

Измерено: Carolyn (`84401340`) и Maureen (`d98ebb4e`) — обе `class_primary='Red'`,
`classroom_id=NULL`. Экран матчит нормализованным ИМЕНЕМ комнаты
([ChildrenPage.tsx:251-258](../../src/pages/children/ChildrenPage.tsx#L251-L258),
[CenterRosterPage.tsx:568-570](../../src/pages/children/CenterRosterPage.tsx#L568-L570)),
поэтому обе видны. SafePass/handoff же ходит по FK — поэтому механика их не видит.

**FK не читает ни одна поверхность в коде.** Это и чинит `20260718d` (Red сейчас)
+ правка StaffSettings UI (остальные).

⚠️ Третья поверхность строже: печатная посещаемость
([AttendanceBlankReport.tsx:103-105](../../src/pages/reports/AttendanceBlankReport.tsx#L103-L105))
матчит `class_primary` СТРОГИМ равенством, без нормализации — молча пустеет при
любом расхождении регистра/пробелов. Спека должна свести все три к одному пути.

### ⚠️ Ловушка миграции text → FK

Наполненность текстового поля выше, чем я показывал по FK:

| Центр | живых | `class_primary` заполнен | FK заполнен | **текст НЕ совпал ни с одной комнатой** |
|---|---|---|---|---|
| Highland | 24 | 24 | 0 | **10** |
| Pearl | 16 | 16 | 0 | **8** |
| Ridge | 35 | 12 | 2 | 0 |

**18 сотрудников** имеют в `class_primary` текст, которому не соответствует ни одна
комната их центра. Они сегодня не попадают ни на один экран класса, и наивная
конвертация «текст → FK» их молча потеряет. Конвертация обязана отчитаться о
несматченных поимённо, а не тихо оставить NULL.

## Ратифицированные решения

- **(а) `classroom_id`:** чинить **StaffSettings UI** — Primary Classroom
  становится выбором комнаты своего центра и пишет FK `classroom_id`
  (+ `class_primary` текстом для совместимости). Остальные 73 наполняют
  директора карточками после правки. Red сейчас = `20260718d`.
- **(б) `position`:** тем же путём — селектор позиции в карточке, наполнение
  директорами. **Гейт lesson plans по позиции ОСТАЁТСЯ** (9 lead'ов + Red
  достаточно для старта).
- План наполнения — **пререквизит массового запуска, но НЕ блокер MVP на Red**.

## Дефолты спеки (до отдельного слова Николая)

- **(д)** ассистенты и вторые учителя в lesson plans = **read-only**
- **(е)** каналы доставки родителям = **PWA push + email-фоллбэк**

## Red — распределение ролей на репетиции

- **Carolyn Hercik** — Lead Teacher → на Red lesson plans получает она
- **Maureen Minadeo** — Assistant Teacher → эталонный кейс «второй учитель»
  для вопроса read-only / без доступа

## Блокеры понедельника

1. **FK** — `20260718d` (Carolyn + Maureen → `classroom_id` Red)
2. **PIN** — после серии, `safepass_set_staff_pin` под логином Николая
   (`pin_hash` пуст у всех 75)

После серии + PIN — контрольный read-back: обе Red-строки проходят
`confirm_handoff`, `pin_hash` непуст → отчёт одним экраном → пн репетиция.
**Выводы репетиции = входные данные спеки.**

## Остаются открытыми

1. (д) ассистенты — подтвердить дефолт read-only или изменить
2. (е) каналы — подтвердить дефолт PWA push + email
3. Чем чинить **18** несматченных `class_primary` при конвертации в FK
