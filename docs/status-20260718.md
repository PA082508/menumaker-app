# Статус 18.07.2026 — пикер продлений, ростер Highland, состояние wave1

Один экран. Всё измерено на живой базе 18.07, read-only. Ничего не применено.

---

## 1. Главное, что развернуло диагноз

Пары A–B со скринов — **не дубли детей. Это сотрудники.**

| Имя | Др. рождения | Строка 1 | Строка 2 |
|---|---|---|---|
| Arkramova Shakhlo | **1997-10-21** | Staff Room | Green Room |
| Ashrabkodjaeva Diloramkhon | **1973-11-05** | Staff Room | без группы |

Все 15 «пар» Highland — взрослые 1962–2007 г.р., загруженные одним импортом 23.06 с разницей 37 секунд: одна строка в Staff Room, вторая в настоящей группе. Отсюда сплошное «no parent record / no email → kiosk» — у сотрудника нет guardian по определению.

**Риск был не «продление мёртвому дублю», а продление сотруднику как ребёнку.**

Настоящий дубль ребёнка во всём ростере **один**: Yabborova Sofiya (Pearl, 2022 г.р.).

## 2. Контаминация пикера

| Центр | Пикер показывал | взрослых | Staff Room | без группы | ушедших с живым флагом |
|---|---|---|---|---|---|
| Highland Heights | 119 | **32** | 17 | 4 | 0 |
| Pearl | 75 | 0 | 15 | 2 | 0 |
| Ridge | 138 | 0 | 0 | 0 | 0 |

Ridge чист. Расхождение канона активности реально, но ушедших с непогашенным флагом сейчас нет ни одного — правка канона профилактическая, сегодняшнее число она не меняет.

## 3. Отдельная находка — взрослые в meal-превью

11 паразитных строк сидят в **настоящих группах** и несут **19 записей `meal_week_records`**. То есть взрослые считались как дети в превью Highland. Прежний фикс (фильтр по имени «Staff Room» в `compute_monthly_claim`) их не ловит — они не в Staff Room.

`monthly_claims` = **0 строк**, поданных клеймов нет → фактический клейм не затронут, затронуты только превью. Гашение строк записи не удаляет; пересчёт превью — отдельное решение. Не делаю молча.

`checkmark export ✅` — экспорт галочек в Sheet ничем из сегодняшнего не тронут.

## 4. wave1 применён не полностью

Шапка `20260717_renewal_wave1.sql` заявляет `✅ APPLIED 2026-07-16`. Верно на 3 секции из 4:

| Секция | В базе |
|---|---|
| A `campaigns` | есть (0 строк) |
| C статус `received` | есть |
| D `refresh_renewal_action_items` | есть (`renewal_scan` = 4) |
| **B `campaign_issues`** | **ОТСУТСТВУЕТ** |

Файл не менялся с 16.07 13:06 — применялась, судя по всему, отредактированная версия. Причину не реконструирую, фиксирую факт. Ровно тот случай из [safepass-anon-tables]: **комментарий миграции — не доказательство.**

Поправка к твоей п.3 посылке: wave1 **закоммичен**, не untracked. Untracked сейчас 5 файлов: `20260706_safepass_device_kiosk`, `20260713_meal_count_door_split_nav`, `20260713_submit_public_form_rpc`, `20260717e_safepass_temp_code_redeem`, `20260718_safepass_deny_teacher_cook_scope`.

## 5. Правка пикера — сделана, ветка `fix/renewal-picker-canon-and-dupes`

[IssueRenewalPage.tsx](../src/pages/enrollment/IssueRenewalPage.tsx):

- **Канон активности** — `.eq(is_active).or(notDepartedBefore(today)).or(date_in)` через [childActive.ts](../src/lib/childActive.ts). Локальной вариации больше нет.
- **Staff-комнаты вырезаны** по `classrooms.is_roster=false` — данные, не эвристика возраста (B отклонён). Снимает 17 строк Highland и 15 Pearl. Оставшиеся 15 взрослых в настоящих группах правятся в данных — см. prepare `b`.
- **Сигнал дублей** читается из `action_items source='duplicate_scan'` (детектор 20260707c/20260708b, ключ `norm_name + center_id`). Свой не пишем.
- **Блокировка выдачи**: чекбокс disabled, select-all исключает, `issue()` отказывает при устаревшем выборе. Бейдж «resolve duplicates first» + счётчик в шапке.

`tsc --noEmit` чист. В мёрж-очередь, не мержу.

## 6. Prepare — готовы, НЕ применены

| Файл | Что | Ждёт |
|---|---|---|
| [`20260718a_campaign_issues_repair.sql`](prepare/20260718a_campaign_issues_repair.sql) | секция B wave1 дословно, идемпотентно | заход (0) |
| [`20260718b_highland_staff_rows.sql`](prepare/20260718b_highland_staff_rows.sql) | гашение **15** (не 32) паразитных строк | заход (4) |
| [`20260718c_dedup_yabborova.sql`](prepare/20260718c_dedup_yabborova.sql) | точечный дедуп одной пары | «размораживай точечно» |

**Поправка по «32 строкам»:** в `menumaker.roster` **нет колонки `is_roster`** — помечать нечем. 17 из 32 уже корректно лежат в Staff Room, их не трогаем. Актуальны 15. Перевести их в Staff Room нельзя — получим по две строки Staff Room на человека, т.е. настоящий дубль; поэтому паразитная строка гасится, keeper = строка Staff Room. У каждого prepare — read-back в шапке и страховка на точное число с откатом.

Дубль Yabborova склеивается тривиально: у него **ноль** ссылок из всех таблиц с `roster_id`/`child_id`. Шага «перенос ссылок» не потребуется.

## 7. Твоя очередь — состояние

Восстановлена с твоих слов, я её не проверял по факту, кроме отмеченного.

- Work Schedule UI обоим Red (Cmd+Shift+R) — фикс `fdc9e22` в `main` подтверждён.
- Применяй-серия по одному с read-back: **(0)** wave1→B ✅ prepare готов · **(1)** `20260717e` temp_codes · **(2)** `20260718` deny_teacher (эталон 272/0/1/0/621) · **(3)** classroom_id двум keeper'ам (`d98ebb4e`, `84401340`) · **(4)** 15 строк ✅ prepare готов
- PIN обоим Red-учителям (`safepass_set_staff_pin` — функция в базе есть, проверил)
- «Флипай» v3 батчем · вт Pearl-день · снятие cook-гранта
- Отдельный go: `feat/enrollment-grouping-phase1`
- Гейты авто-файла: commit OFF до DOB-захода И сведения countersign-источников. **`renewal_countersign_types` в базе отсутствует** — список A взять неоткуда, гейт закрыт по факту.
- Пн 20.07 Ridge-репетиция SafePass (Carolyn + Maureen)
- Заморозка держится; точечные исключения — только 20260718c.

## 8. Открыто

- Опечатка в имени — жду, какое и как правильно.
- Пересчёт meal-превью Highland после гашения 15 строк — нужен ли.
- A (перенос сотрудников `roster`→`staff`, он же staff-pseudoclass) — в бэклоге.
