// avatarSizes.ts — ОДИН размерный ряд лиц на всё приложение.
//
// ПОВОД (владелец, 06.08): лица учителей и родителей были мельче детских —
// 34–38px против 72px у ребёнка. Лицо в 36px читается как инициалы: у двери
// сравнивают ЧЕЛОВЕКА, а не кружок. Прежде размеры стояли числами по месту и
// уже разъезжались (24 · 30 · 34 · 36 · 38 · 44 · 52 · 60 · 72), поэтому ряд
// вынесен СЮДА: одни константы, не копии.
//
// Что НЕ трогается: печать (бланки — реплики, там свои координаты) и плотные
// рабочие сетки счёта питания, где лицо стоит рядом с чекбоксом. И шрифты:
// заказ был про лица, а не про кегль.
export const AVATAR = {
  /** Плитка в сетке карточек — размер, ратифицированный для детей 16.07. */
  tile: 72,
  /** Строка списка: лист Родители, регистрация родителя, будущая карточка родителя. */
  row: 48,
  /** Шапка карточки человека. */
  header: 60,
} as const
