import { useState } from 'react'
const C = { bg:'#0f1117',surface:'#1a1d27',surface2:'#22263a',border:'#2e3350',text:'#f0f2ff',muted:'#7b82a6',green:'#00e896',greenDim:'rgba(0,232,150,0.12)',amber:'#ffb740',amberDim:'rgba(255,183,64,0.12)',red:'#ff4d6a',blue:'#5b8bff' }
type S='overview'|'regular'|'early'|'late'|'transport'|'rules'
const STEPS={regular:[{icon:'1️⃣',title:'Select Your Classroom',desc:'Choose your classroom from the dropdown. The system shows only children enrolled in your class.'},{icon:'2️⃣',title:'Wait for Incoming Requests',desc:"When a parent taps Drop Off or Pick Up, their child's name appears in the Incoming Requests panel."},{icon:'3️⃣',title:'Physically Receive the Child',desc:'Go to the parent. Physically take the child into your care. The child must be in your hands before you tap Accept.'},{icon:'4️⃣',title:'Tap ACCEPT',desc:'Once the child is physically with you, tap ACCEPT. This creates a legal timestamp. The parent receives ✅ on their phone.'},{icon:'5️⃣',title:'Pick-Up: Verify the Person',desc:'For pick-up, check the person is listed as Authorized. If unrecognized, request photo ID. If not authorized, do not release — call your director.'},{icon:'6️⃣',title:'Physically Hand Over the Child',desc:"Place the child physically into the authorized person's hands. Only then tap RELEASE."},{icon:'7️⃣',title:'Tap RELEASE',desc:'Tap RELEASE. The parent receives confirmation. The handoff is legally recorded.'}],early:[{icon:'1️⃣',title:'Switch to Early Care Mode',desc:'Tap ☀️ Early Care at the top. You will see children from ALL classrooms who have arrived before their Lead Teacher.'},{icon:'2️⃣',title:'Receive Each Child',desc:'Accept each child normally. Children appear in your list with a timer showing how long they have been waiting.'},{icon:'3️⃣',title:'Monitor the Ratio',desc:'Ohio minimums apply in Early Care. Ratio is set by the youngest child present. Watch for yellow and red warnings.'},{icon:'4️⃣',title:'Transfer to Class',desc:'When a Lead Teacher arrives, tap Transfer to Class for each child in their class.'},{icon:'5️⃣',title:'Escalate if Needed',desc:'15+ min waiting → tap Call. 45+ min → tap CPS and notify your director immediately.'}],late:[{icon:'1️⃣',title:'Switch to Late Care Mode',desc:'Tap 🌙 Late Care when closing time has passed and children remain.'},{icon:'2️⃣',title:'Shift Cannot Close',desc:'The system blocks shift close while children are present. You are responsible for every child on your screen.'},{icon:'3️⃣',title:'15 Minutes — Call Parent',desc:'Call the parent directly. Tap 📞 Call to log this action.'},{icon:'4️⃣',title:'30 Minutes — Alert Director',desc:'Tap 📋 Director. Your director will contact the parent and emergency contacts.'},{icon:'5️⃣',title:'45 Minutes — Emergency',desc:'Tap 🚨 911. Director contacts all emergency persons.'},{icon:'6️⃣',title:'60 Minutes — CPS',desc:'Tap 🚨 CPS. Director initiates Ohio CPS notification. Stay with the child.'},{icon:'7️⃣',title:'Parent Arrives',desc:'Tap ✓ Parent Arrived. Verify identity. Hand over child. System records the time.'}],transport:[{icon:'1️⃣',title:'Switch to Transport Mode',desc:"Tap 🚌 Transport to see today's school runs."},{icon:'2️⃣',title:'Point 1 — Center to Driver',desc:'Check each child name. Tap Boarded for each child before the vehicle departs.'},{icon:'3️⃣',title:'Point 2 — Driver to School',desc:"Driver completes checklist at school drop-off with GPS timestamp. Play Academy responsibility ends at the school's designated drop-off point."},{icon:'4️⃣',title:'Point 3 — School to Driver',desc:'School staff confirms children boarding the return vehicle.'},{icon:'5️⃣',title:'Point 4 — Driver to Center',desc:'Confirm each child returned. Tap ✓ Confirm All Children Returned to complete the run.'}]}
const RULES=[{title:'Physical handoff only',desc:'Responsibility transfers at physical handoff — not when a parent enters the building. The child must be in your hands (Accept) or in their hands (Release) before you tap.'},{title:'Never tap in advance',desc:'Do not tap Accept before the child is with you. Do not tap Release before handing the child over. Your tap is your legal signature.'},{title:'Unrecognized persons',desc:'Request a government-issued photo ID. If the person is NOT on the authorized list, do not release the child — call your director.'},{title:'Unknown phone alert',desc:'If the system flags an unregistered device, do not release the child. Contact your director.'},{title:'Ratio compliance',desc:'Monitor your child count. The system shows warnings when you approach your classroom limit. In Early Care, Ohio minimum ratios apply.'},{title:'Shift close',desc:'You cannot close your shift while children are present. Every child must be transferred or picked up before your shift ends.'}]
export default function SafePassHelpPage(){
  const[s,setS]=useState<S>('overview')
  const nb=(v:S,l:string)=><button key={v} onClick={()=>setS(v)} style={{padding:'6px 14px',borderRadius:20,fontSize:12,fontWeight:600,cursor:'pointer',fontFamily:'inherit',border:'none',background:s===v?C.green:C.surface2,color:s===v?C.bg:C.muted}}>{l}</button>
  const sc=(step:{icon:string;title:string;desc:string},i:number)=><div key={i} style={{background:C.surface,border:`1.5px solid ${C.border}`,borderRadius:12,padding:'16px 18px',marginBottom:10}}><div style={{display:'flex',gap:14,alignItems:'flex-start'}}><div style={{fontSize:24,flexShrink:0}}>{step.icon}</div><div><div style={{fontWeight:700,fontSize:14,color:C.text,marginBottom:4}}>{step.title}</div><div style={{fontSize:13,color:C.muted,lineHeight:1.6}}>{step.desc}</div></div></div></div>
  return<div style={{minHeight:'100vh',background:C.bg,color:C.text,fontFamily:"'Inter','DM Sans',sans-serif"}}>
    <div style={{background:C.surface,borderBottom:`1px solid ${C.border}`,padding:'16px 20px',display:'flex',alignItems:'center',gap:12}}>
      <div style={{width:36,height:36,borderRadius:10,background:C.green,display:'flex',alignItems:'center',justifyContent:'center',fontSize:18}}>🔒</div>
      <div><div style={{fontWeight:800,fontSize:16}}>SafePass — Teacher Guide</div><div style={{fontSize:11,color:C.muted}}>Play Academy Wickliffe · Step-by-step instructions</div></div>
      <button onClick={()=>window.print()} style={{marginLeft:'auto',padding:'8px 16px',borderRadius:8,background:C.surface2,color:C.muted,border:`1px solid ${C.border}`,cursor:'pointer',fontSize:12,fontFamily:'inherit'}}>🖨 Print</button>
    </div>
    <div style={{display:'flex',gap:6,flexWrap:'wrap' as const,padding:'12px 20px',background:C.surface,borderBottom:`1px solid ${C.border}`}}>
      {nb('overview','📋 Overview')}{nb('regular','✅ Regular Day')}{nb('early','☀️ Early Care')}{nb('late','🌙 Late Care')}{nb('transport','🚌 Transport')}{nb('rules','⚖️ Rules')}
    </div>
    <div style={{maxWidth:720,margin:'0 auto',padding:'24px 20px'}}>
      {s==='overview'&&<div>
        <div style={{fontSize:20,fontWeight:800,marginBottom:8}}>How SafePass Works</div>
        <div style={{fontSize:14,color:C.muted,marginBottom:24,lineHeight:1.7}}>SafePass records every child transfer. Your tap of Accept or Release is a legal timestamp. Parents receive instant confirmation on their phones.</div>
        <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12,marginBottom:24}}>
          {[{icon:'✅',mode:'Regular',color:C.green,desc:'Normal daily drop-off and pick-up. Your class only.'},{icon:'☀️',mode:'Early Care',color:C.amber,desc:'Before classes — duty teacher manages all arriving children.'},{icon:'🌙',mode:'Late Care',color:C.blue,desc:'After closing — escalation protocol for delayed parents.'},{icon:'🚌',mode:'Transport',color:C.muted,desc:'School-age bus runs — 4 transfer points.'}].map(m=><div key={m.mode} style={{background:C.surface,borderRadius:12,padding:'14px 16px',border:`1.5px solid ${C.border}`}}><div style={{fontSize:22,marginBottom:6}}>{m.icon}</div><div style={{fontWeight:700,fontSize:13,color:m.color,marginBottom:4}}>{m.mode}</div><div style={{fontSize:12,color:C.muted,lineHeight:1.5}}>{m.desc}</div></div>)}
        </div>
        <div style={{background:'rgba(255,77,106,0.08)',border:`1.5px solid ${C.red}`,borderRadius:12,padding:'16px 18px'}}><div style={{fontWeight:800,fontSize:14,color:C.red,marginBottom:8}}>⚖️ The One Rule That Never Changes</div><div style={{fontSize:13,color:C.text,lineHeight:1.7}}><strong>Responsibility transfers at physical handoff — not at the door.</strong><br/>Only the physical handoff + your tap creates the legal transfer.</div></div>
      </div>}
      {s==='regular'&&<div><div style={{fontSize:20,fontWeight:800,marginBottom:4}}>✅ Regular Day</div><div style={{fontSize:13,color:C.muted,marginBottom:20}}>Your classroom, your children, one by one.</div>{STEPS.regular.map(sc)}</div>}
      {s==='early'&&<div><div style={{fontSize:20,fontWeight:800,marginBottom:4}}>☀️ Early Care Mode</div><div style={{fontSize:13,color:C.muted,marginBottom:8}}>Before classes — you receive children from all classrooms.</div><div style={{background:C.amberDim,border:`1px solid ${C.amber}`,borderRadius:10,padding:'10px 14px',fontSize:12,color:C.amber,marginBottom:20}}>⚠️ Ohio minimum ratios apply. Ratio set by youngest child present.</div>{STEPS.early.map(sc)}</div>}
      {s==='late'&&<div><div style={{fontSize:20,fontWeight:800,marginBottom:4}}>🌙 Late Care Mode</div><div style={{fontSize:13,color:C.muted,marginBottom:8}}>After closing — escalation at 15, 30, 45, 60 minutes.</div><div style={{background:'rgba(255,77,106,0.08)',border:`1px solid ${C.red}`,borderRadius:10,padding:'10px 14px',fontSize:12,color:C.red,marginBottom:20}}>🔒 Shift CANNOT close while children are present.</div>{STEPS.late.map(sc)}</div>}
      {s==='transport'&&<div><div style={{fontSize:20,fontWeight:800,marginBottom:4}}>🚌 Transport Mode</div><div style={{fontSize:13,color:C.muted,marginBottom:8}}>4 transfer points — every child accounted for.</div><div style={{background:C.greenDim,border:`1px solid ${C.green}`,borderRadius:10,padding:'10px 14px',fontSize:12,color:C.green,marginBottom:20}}>Per Ohio law, responsibility ends at the school drop-off point.</div>{STEPS.transport.map(sc)}</div>}
      {s==='rules'&&<div><div style={{fontSize:20,fontWeight:800,marginBottom:4}}>⚖️ Rules & Legal Notes</div><div style={{fontSize:13,color:C.muted,marginBottom:20}}>Every rule is grounded in Ohio law and Play Academy policy.</div>{RULES.map((r,i)=><div key={i} style={{background:C.surface,border:`1.5px solid ${C.border}`,borderLeft:`4px solid ${C.green}`,borderRadius:12,padding:'14px 18px',marginBottom:10}}><div style={{fontWeight:700,fontSize:13,color:C.green,marginBottom:6}}>{r.title}</div><div style={{fontSize:13,color:C.muted,lineHeight:1.6}}>{r.desc}</div></div>)}</div>}
    </div>
  </div>
}
